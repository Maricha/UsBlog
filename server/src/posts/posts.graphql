type Query {
  post(id: ID!): Post
  getPosts: [Post]
}

type Mutation {
  createPost(createPostInput: CreatePostInput): Post
}

type Subscription {
  postCreated: Post
}

type Post {
  id: Int
  title: String
  text: String
  comments: [Comment]
  tags: [Tag]
}

input CreatePostInput {
  title: String
  text: String
  tagsId: [Int]
}